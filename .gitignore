# downloaded Tesseract language models (bin/fetch-tessdata recreates them)
/tessdata/
# rendered page images and per-page OCR results (regenerated by bin/ocr-pdf)
/work/
# Python bytecode (bin/ocr-photo is also loaded as a module by experiments/)
__pycache__/
# the scanned PDF and the fine-tuned Kraken model: large, kept out of git (and Git LFS) on purpose
/pdf/
/models/
/photos
